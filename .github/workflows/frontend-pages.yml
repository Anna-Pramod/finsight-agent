# Deploy the static Next.js frontend to GitHub Pages.
# Placeholder skeleton — real build/deploy implemented in Issue #16.
name: frontend-pages

on:
  workflow_dispatch:
  # push:
  #   branches: [main]
  #   paths: ["frontend/**", ".github/workflows/frontend-pages.yml"]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "TODO(#16): npm ci && npm run build with output:'export'"
          echo "then upload ./frontend/out and deploy to GitHub Pages"

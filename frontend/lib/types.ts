// Shared TypeScript types for the frontend. Populated in Issue #15+.
// These mirror the backend Pydantic schemas (chat, finance, audit).

// TODO(#15): define Persona, ChatRequest, ChatResponse, ToolTrace, RiskSignal, etc.
export {};
